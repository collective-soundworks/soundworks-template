// note: we should be able to modify this file while still being able to safely
// inject new schemas registrations programmatically
export async function schemasLoader(server) {
  // import and register new schemas following the boilerplate bellow
  // const { default: globals } = await import('./globals.js');
  // server.stateManager.registerSchema('globals', globals);
}
